AWSTemplateFormatVersion: '2010-09-09'

Resources:
  MyVPC:
    Type: AWS::EC2::VPC
    DeletionPolicy: Retain
    Properties:
      CidrBlock: 10.0.0.0/16   # replace with your CIDR
      EnableDnsSupport: true
      EnableDnsHostnames: true
